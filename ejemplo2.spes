library io;
library graph;

class caso1;
	def A, B, C as int;
	def cad as string;

	main ()
                		read(cad);
		write("Mensaje: ", cad);
		write("Dame 2 valores ");
		read (A, B);
		C = A * B;
		write ("Primer Resultado: ", C);
		C += A ** B;
		write ("Segundo Resultado: ", C);
	endmain
endclass