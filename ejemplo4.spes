library io;
library graph;

class caso1;
	def fibo, num, fibo2 as int;

	main ()
		write("Sigamos Probando!");
		write("Calcula fibonacci de: ");
		read (num);
		if ( num == 1 || num == 2)
			write ("Fibonacci: 1 ");
		endif;
                                     fibo = num;
                                     while (num >= 2)
			--num;
			fibo += num;
    		endwhile;
		write ("Fibonacci: ", fibo);
	endmain
endclass